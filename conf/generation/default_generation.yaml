# this is the default for llama 2, its a copy of temp_06.yaml
kwargs:
  do_sample: True
  top_k: 10
  num_return_sequences: 1
  max_new_tokens: 600
  temperature: 0.6 # main param for sweep in temperature based sampling
label: temp_06